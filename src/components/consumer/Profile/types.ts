export interface IProfileFormValues {
  first_name: string;
  last_name: string;
  email: string;
  phone: string;
  dob: string;
  currentPassword?: string;
  newPassword?: string;
  confirmNewPassword?: string;
}
